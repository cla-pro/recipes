<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>ch.lavanchy</groupId>
        <artifactId>recipes</artifactId>
        <version>2.0.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <groupId>ch.lavanchy.recipes</groupId>
    <artifactId>recipes-db</artifactId>
    <name>Recipes seeker DB</name>
    <packaging>jar</packaging>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <db.url>toBeDefinedWithinAProfile</db.url>
        <db.username>toBeDefinedWithinAProfile</db.username>
        <db.password>toBeDefinedWithinAProfile</db.password>
    </properties>

    <profiles>
        <profile>
            <id>cheznous</id>
            <properties>
                <db.url>jdbc:mariadb://cheznous:3306/recipes</db.url>
                <db.username>recipes_dba</db.username>
                <db.password>recipesdba</db.password>
            </properties>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.liquibase</groupId>
                <artifactId>liquibase-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>updateSQL</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <changeLogFile>src/main/resources/liquibase/database.xml</changeLogFile>
                    <driver>org.mariadb.jdbc.Driver</driver>
                    <migrationSqlOutputFile>${project.build.directory}/liquibase-${project.version}.sql</migrationSqlOutputFile>
                    <url>${db.url}</url>
                    <username>${db.username}</username>
                    <password>${db.password}</password>
                    <outputFileEncoding>UTF-8</outputFileEncoding>
                    <outputDefaultSchema>false</outputDefaultSchema>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-maven-plugin</artifactId>
        </dependency>
        <dependency>
            <groupId>org.mariadb.jdbc</groupId>
            <artifactId>mariadb-java-client</artifactId>
        </dependency>
    </dependencies>

</project>